<%- include('includes/head.ejs') %>
  <body>
    <h1>Users List:</h1>
    <% if (users.length > 0) { %>
      <ul>
        <% for (let user of users) {%>
        <li><%= user.name %></li>
        <% } %>
      </ul>
    <% } else { %>
    <% } %>
  </body>

</html>